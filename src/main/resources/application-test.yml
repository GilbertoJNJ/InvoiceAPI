spring:
  application:
    name: InvoiceAPI
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    open-in-view: true

client:
  logistock:
    url: logistock-url